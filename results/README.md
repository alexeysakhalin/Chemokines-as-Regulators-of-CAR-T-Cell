# Generated results

This directory is populated by the analysis workflow and is not versioned. Each run writes validated source tables, figures, workflow logs, stage markers, a provenance record, and output checksums beneath the configured output directory.

Only `.gitkeep` and this explanatory file are tracked. Scientific outputs must be regenerated from checksum-verified inputs and the recorded configuration.
